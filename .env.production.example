# ===============================================
# RAILWAY ENVIRONMENT VARIABLES
# ===============================================
# Copy these to your Railway project settings
# ===============================================

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000
API_VERSION=v1

# Database (Railway provides this automatically via PostgreSQL plugin)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Encryption Configuration (CHANGE THESE IN PRODUCTION!)
# Algorithm for encryption (aes-256-cbc recommended)
ALGORITHM=aes-256-cbc
# Secret key must be 64 hex characters (32 bytes)
ENCRYPT_SECRET_KEY=generate-your-own-64-char-hex-string-here-use-openssl-rand-hex-32
# IV must be 32 hex characters (16 bytes)
ENCRYPT_IV=generate-your-own-32-char-hex-string-here-use-openssl-rand-hex-16

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
# JWT secret should be at least 32 characters
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-chars
JWT_EXPIRATION_TIME=7d

# CORS Configuration
# Add your Vercel frontend URL here
CORS_ORIGIN=https://your-app.vercel.app

# URL Shortener Configuration
BASE_URL=https://your-app.railway.app
SHORT_URL_CHAR_SIZE=6

# Logging
LOG_LEVEL=info

# TypeORM Configuration (production defaults)
TYPEORM_SYNCHRONIZE=false
TYPEORM_MIGRATIONS_RUN=true
TYPEORM_MIGRATIONS=dist/database/migrations/*.js

# ===============================================
# HOW TO GENERATE SECURE KEYS:
# ===============================================
# ENCRYPT_SECRET_KEY (64 hex chars):
#   openssl rand -hex 32
#
# ENCRYPT_IV (32 hex chars):
#   openssl rand -hex 16
#
# JWT_SECRET (random string):
#   openssl rand -base64 32
# ===============================================
